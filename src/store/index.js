import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    // 상태(state) 변수 정의
  },
  mutations: {
    // 변이(mutations) 함수 정의
  },
  actions: {
    // 액션(actions) 함수 정의
  },
  getters: {
    // 게터(getters) 함수 정의
  }
});